${STATUS_EMOJI} Drone CI #${DRONE_BUILD_NUMBER} (${DRONE_BUILD_LINK}) of ${DRONE_REPO} ${DRONE_BUILD_STATUS}
${GIT_COMMITTER_USERNAME} committed ${GIT_SHA_SHORT} (${DRONE_COMMIT_REF}):
${GIT_COMMIT_MESSAGE}