${STATUS_EMOJI} GitHub Action #${GITHUB_RUN_NUMBER} (https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}) of ${GITHUB_REPOSITORY} ${GITHUB_JOB_STATUS}
${GIT_COMMITTER_USERNAME} committed ${GIT_SHA_SHORT} (${GITHUB_REF}):
${GIT_COMMIT_MESSAGE}