🛎 Github action #${GITHUB_RUN_NUMBER} (https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}) of ${GITHUB_REPOSITORY} ${GITHUB_JOB_STATUS}
Commit (${GITHUB_SHA_SHORT}) on ${GITHUB_REF} by ${GIT_COMMITTER_USERNAME}:
${GIT_COMMIT_MESSAGE}