🛎 Action #${GITHUB_RUN_NUMBER} trigger by ${GITHUB_ACTOR} ${GITHUB_JOB_STATUS}.

📝 Commit by ${GIT_COMMITER_USERNAME} on ${GITHUB_REF} of ${GITHUB_REPOSITORY}:
  ${GIT_COMMIT_MESSAGE}

🌐 https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}
